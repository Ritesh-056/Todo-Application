import 'package:flutter/material.dart';



class TodoCardItem extends StatelessWidget {
  const TodoCardItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //will reflect single  todos items here
    return Container();
  }
}
